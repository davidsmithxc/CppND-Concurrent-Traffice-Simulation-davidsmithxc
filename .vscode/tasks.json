{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-release",
            "type": "shell",
            "command": "cd ${workspaceFolder}/build && make -j32",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}